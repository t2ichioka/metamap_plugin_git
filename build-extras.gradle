ext.postBuildExtras = {
    allprojects {
        repositories {
             google()
             mavenCentral()
             maven {
                 url = uri("${rootDir.absolutePath}/app/sdk/maven-repository")
            }
        }
    }
    dependencies {
        val metamapVersion = rootDir.resolve("app/sdk/VERSION").readText().trim()
        implementation("jp.metamaps.positioning:metamap-mapview:$metamapVersion")
    }
}